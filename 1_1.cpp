// A typical c++ code template for competitive programming

#include <bits/stdc++.h>

using namespace std;

int main(void){
  ios::sync_with_stdio(0);
  cin.tie(0); // input and output is a bottle neck in program, these lines make input and output more efficient
  
  int a,b;
  string x;
  cin >> a >> b >> x; // taking multiline inputs
  
  // Working with numbers
  
  // Note : remainder can be taken before operations in addn, subn and multiplication
  // eg (a+b) mod m = (a mod m + b mod m) mod m
  // Example : calculate mod of n!
//  int m = 2;
//  long long x = 1;
//  for (int i = 2; i <= n; i++){
//    x = (x*i)%m;
//  }
//  cout << x%m << "\n";
//  return 0;
}